from setuptools import setup, find_packages

setup(name='log_cleaner',
    version='1.0.0',
    description='clean outdated log.',
    packages=find_packages(),
    author='lisfe',
    author_email='lisfegm@gmail.com',
    install_requires=[]
    )
